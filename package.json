{
  "name": "toolnova",
  "version": "1.0.0",
  "private": true,
  "description": "ToolNova — a self-growing collection of free browser-based tools. Zero-dependency static site generator + daily autonomous tool pipeline.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "add": "node scripts/add-daily-tool.mjs",
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "test": "node --test tests/",
    "all": "node --test tests/ && node scripts/build.mjs && node scripts/verify.mjs",
    "dev": "node scripts/build.mjs && cd dist && python3 -m http.server 4173"
  },
  "license": "MIT"
}
